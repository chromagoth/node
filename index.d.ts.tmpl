/* @chromagoth/node — generated by chromagoth CLI — do not edit */

export type ColorSlot =
{{ range $i, $s := .slots }}  | '{{ $s }}'
{{ end }};

export type Variant =
{{ range $i, $p := .palettes }}  | '{{ $p.variant }}'
{{ end }};

export interface Palette {
  name: string;
  variant: Variant;
  mascot: string;
  style: string;
  vibe: string;
  dark: boolean;
  status: string;
  colors: Record<ColorSlot, string>;
}

export declare const palettes: Palette[];
export declare const themes: Record<Variant, Palette>;
