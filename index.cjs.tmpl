/* @chromagoth/node — generated by chromagoth CLI — do not edit */
'use strict';

const palettes = {{ toJSON .raw }};

const themes = Object.fromEntries(palettes.map(p => [p.variant, p]));

module.exports = { palettes, themes };
